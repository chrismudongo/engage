<%= form_with(model: survey, local: true) do |form| %>
  <% if survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% survey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :group_id %><br>
    <%= form.text_field :group_id %>
  </div>

  <%= form.fields_for :question do |q_form| %>

         <p class="inline">
           <%= q_form.label :qtype %>
           <%= q_form.text_field :qtype %>
         </p>

         <p class="inline">
           <%= q_form.label :tittle %>
           <%= q_form.text_field :tittle %>
         </p>

         <p class="inline">
           <%= q_form.label :Q %>
           <%= q_form.text_field :Q %>
         </p>

   <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
